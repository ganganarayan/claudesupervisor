<Window x:Class="ClaudeSupervisor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Claude Supervisor"
        Height="560" Width="880"
        MinHeight="380" MinWidth="640"
        WindowStartupLocation="CenterScreen"
        Background="#F4F6FA"
        FontFamily="Segoe UI">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBlock Text="Claude Supervisor"
                       FontSize="22" FontWeight="SemiBold"
                       Foreground="#1B2430" VerticalAlignment="Center" />
            <TextBlock x:Name="VersionText"
                       Text=""
                       FontSize="12" Foreground="#7A8598"
                       VerticalAlignment="Bottom" Margin="10,0,0,4" />
        </StackPanel>

        <!-- Toolbar -->
        <Grid Grid.Row="1" Margin="0,0,0,12">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                <Button x:Name="RefreshButton" Content="Refresh" Click="RefreshButton_Click" />
                <Button x:Name="TerminateButton" Content="Terminate Selected"
                        Click="TerminateButton_Click" Background="#D9534F" />
                <CheckBox x:Name="AutoRefreshCheck" Content="Auto-refresh (2s)"
                          VerticalAlignment="Center" Margin="6,0,0,0"
                          Checked="AutoRefreshCheck_Changed"
                          Unchecked="AutoRefreshCheck_Changed" />
            </StackPanel>
            <TextBox x:Name="FilterBox"
                     Width="220" HorizontalAlignment="Right"
                     VerticalContentAlignment="Center" Padding="6,4"
                     TextChanged="FilterBox_TextChanged"
                     ToolTip="Filter by process name (default: claude, node, anthropic)" />
        </Grid>

        <!-- Process grid -->
        <Border Grid.Row="2" BorderBrush="#DCE1EA" BorderThickness="1" CornerRadius="6"
                Background="White">
            <DataGrid x:Name="ProcessGrid"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal"
                      HorizontalGridLinesBrush="#EEF1F6"
                      BorderThickness="0"
                      Background="White"
                      RowBackground="White"
                      AlternatingRowBackground="#FAFBFD"
                      SelectionMode="Extended">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="PID" Binding="{Binding Pid}" Width="70" />
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*" />
                    <DataGridTextColumn Header="Memory (MB)" Binding="{Binding MemoryMb}" Width="110" />
                    <DataGridTextColumn Header="Threads" Binding="{Binding Threads}" Width="80" />
                    <DataGridTextColumn Header="Started" Binding="{Binding StartTime}" Width="150" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- Status bar -->
        <TextBlock x:Name="StatusText" Grid.Row="3"
                   Margin="2,10,0,0" FontSize="12" Foreground="#7A8598"
                   Text="Ready." />
    </Grid>
</Window>
